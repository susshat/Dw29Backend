type ="module" , .js  equal to .mjs


root directory => the folder that contains package.json, always open this folder

json =>javascript object notaion, it an object whose keys are wraped in double quotes, it doesnt include comma at the end.


backend ==> making api 

WORK (crud)
c(create) => POST
r(read)   => GET
u(update) => PATCH
d(delete) => delete

frontend makes request to backend,
backend preforms task as per the request

to request 
    url 
    method => post | get | patch | delete

work of backend
    is making api
    making api means defining for a particular request

Route

to get the data sent in the body we use req.body
for dynamic route ==> req.params
everything in the URL is  string""

Middleware
    it is a function which has req,res,next
    based on parameter there is two types of Middleware
        normal Middleware
            (req,res,next) ={}
            next() is used to call normal Middleware

        Error middle ware 
            (err,req,res,next) => {}
            next(error) is used to call error middleware

    based on location,
        route middleware
        application middleware => it will run for every route


    what hapens when api is hit
        itll execute middleware from application (main file)
        when api is hit first it executes normal middleware

tackle import   

 let k = a.b("c",e.f.g, h.i()) ==> import a , e ,h

 Status code 
 success 2XX
    create/update => 201        
    other => 200
    consfusion => 200
 error 4XX
    consfusion =>400
    notfount=>404